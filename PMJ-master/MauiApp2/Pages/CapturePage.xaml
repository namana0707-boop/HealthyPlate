<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.Pages.CapturePage"
             Title="">

    <Grid RowDefinitions="Auto,*,Auto,Auto" Padding="12">

        <Grid Grid.Row="1">
            <Image x:Name="Preview" Aspect="AspectFit"/>
            <GraphicsView x:Name="Overlay" HorizontalOptions="Fill" VerticalOptions="Fill" ZIndex="1" InputTransparent="True" />
        </Grid>

        <HorizontalStackLayout Grid.Row="2" Spacing="12">
            <Button Text="📸 사진 찍기" Clicked="OnCapture" Margin="0,10,0,0" BackgroundColor="#2E7D32"/>
            <Button x:Name="GalleryBtn" Text="📁 갤러리에서 선택" Clicked="OnPickFromGallery" Margin="0,10,0,0" BackgroundColor="#2E7D32"/>
            <Button x:Name="AnalyzeBtn"
                    Text="🔍 음식 분석하기"
                    Clicked="OnAnalyze"
                    IsEnabled="False"
                    Margin="0,10,0,0">
                <Button.Triggers>
                    <Trigger TargetType="Button" Property="IsEnabled" Value="True">
                        <Setter Property="BackgroundColor" Value="#2E7D32"/>
                        <Setter Property="TextColor" Value="White"/>
                    </Trigger>
                    <Trigger TargetType="Button" Property="IsEnabled" Value="False">
                        <Setter Property="BackgroundColor" Value="#CCCCCC"/>
                        <Setter Property="TextColor" Value="#666666"/>
                    </Trigger>
                </Button.Triggers>
            </Button>
            <Button x:Name="FeedbackBtn"
                    Text="💬 피드백 보기"
                    Clicked="OnShowFeedback"
                    IsEnabled="False"
                    Margin="0,10,0,0">
                <Button.Triggers>
                    <Trigger TargetType="Button" Property="IsEnabled" Value="True">
                        <Setter Property="BackgroundColor" Value="#2E7D32"/>
                        <Setter Property="TextColor" Value="White"/>
                    </Trigger>
                    <Trigger TargetType="Button" Property="IsEnabled" Value="False">
                        <Setter Property="BackgroundColor" Value="#CCCCCC"/>
                        <Setter Property="TextColor" Value="#666666"/>
                    </Trigger>
                </Button.Triggers>
            </Button>

        </HorizontalStackLayout>

        <Border Grid.Row="3" StrokeThickness="0" BackgroundColor="#11000000" StrokeShape="RoundRectangle 12">

        </Border>
    </Grid>
</ContentPage>